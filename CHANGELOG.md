# Changelog

## Unreleased

- `standard` 提供固定简体中文脚手架，删除 Admin、共享包和 Server 的国际化运行模块及动态消息管理接口。
- 不兼容变化：菜单标题直接存储显示文本，采用面向新库的独立 `baseline-2`，不支持旧国际化数据库原地升级。
- 使用独立浏览器存储命名空间，避免旧语言偏好及消息键标签页进入 standard；首次进入需要重新登录。

## 1.0.0-rc.1

- 建立独立的 Gnilc Auth 产品版本，统一 Admin、Server 和全部内部模块的版本。
- 提供管理员认证、会话、RBAC、管理员管理与动态国际化。
- 新增版本一致性检查、完整发布验收、带校验和的发布包与手动发布工作流。
- 产品版本与浏览器存储结构版本解耦；首次迁移会重新建立本地存储，需要重新登录。
- 建立新数据库基线和不可变迁移清单；尚不提供从历史未标记数据库自动升级的路径。
